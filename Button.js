import React from 'react';

export default class FancyButton extends React.Component {
  render() {
    return (
      <View>
        <Text>HERE NEW STUFF!</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
